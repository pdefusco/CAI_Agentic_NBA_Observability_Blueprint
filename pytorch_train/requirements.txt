pandas==2.1.3
pyarrow==14.0.1
Faker==20.1.0
setuptools>=75.8.1

scikit-learn>=1.6.1
torch>=2.2.0
onnx>=1.16.0
onnxruntime==1.21.0
onnxscript>=0.1.0
# NOTE: mlflow is intentionally NOT listed — the Cloudera AI runtime ships a
# compatible client that talks to the CAI tracking server.  Installing a newer
# client into ~/.local shadows it and can break run creation.
